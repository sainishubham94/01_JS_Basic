console.log('shubhamm saini')

//VARIABLES

const accoundID = 12343
let email = 'saini@google.com'
var name = 'shubham '
let password = '12345'
let phoneNumber;

// prefer not to use va because if we want to change one variuable name which has name in other file also thatchanges the other variable also with the same name
console.log(email);
console.table([email, password, accoundID, phoneNumber] )